<h1>My Tasks</h1>
<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task)%></li>
  <% end %>
</ul>
 <input <%link_to "new_task" task_path(@task) %> type="submit" name="commit" value="new" data-disable-with="Update Restaurant">

